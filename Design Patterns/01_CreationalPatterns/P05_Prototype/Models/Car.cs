namespace P05_Prototype.Models
{
    public class Car : Vehicle
    {
        public Car(string model, CarEngine engine, string color) 
            : base(model, engine, color)
        {
        }
    }
}
